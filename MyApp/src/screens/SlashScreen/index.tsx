import * as React from 'react';
import {Text, View, StyleSheet, Image} from 'react-native';
import {LOGO_APP} from '../../assets';

const SlashScreen = () => {
  return (
    <View>
      <Image source={LOGO_APP} />
    </View>
  );
};

export default SlashScreen;
